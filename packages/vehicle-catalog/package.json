{
  "name": "@immatout/vehicle-catalog",
  "version": "0.1.0",
  "private": true,
  "description": "Catalogue véhicules (ADEME Car Labelling + NHTSA vPIC) : marque → modèle → motorisation → specs (CV fiscaux, CO2 WLTP, poids, énergie).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build-catalog": "tsx scripts/build-catalog.ts",
    "lint": "eslint \"src/**/*.ts\" \"scripts/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "echo \"no tests in @immatout/vehicle-catalog\""
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
